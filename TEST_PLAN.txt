1. Start the app with npm start.
2. Confirm the global “Historical scoring test mode” banner is gone.
3. Open Release Readiness and confirm “Live NHL scoring mode is active.”
4. Use Regenerate Projection once.
5. Confirm the shared projection metadata reports Projection Version 6.
6. Open a newly created cycle/window and compare the manager-facing projection:
   - a forward raw projection of 40.0 should freeze at 38.0;
   - a defense raw projection of 40.0 should freeze at 37.2;
   - a goalie-unit raw projection of 100.0 should freeze at 90.0.
7. Confirm an existing active window retains its old frozen projection.
8. Run the per-slot projection simulator and confirm 10 / 10 checks pass.
9. Run npm run build and confirm bundle generation completes.
10. After enough completed windows, compare Projection Accuracy by version.
    Version 6 should be closer to the requested ~70% under-projection target.
