BANKED PLAYOFF WINDOWS V1 — VALIDATION

PASSED
- TypeScript project checking:
  npx tsc --noEmit -p tsconfig.app.json

- Angular template/type compilation:
  ./node_modules/.bin/ngc -p tsconfig.app.json

- Deterministic playoff-window simulator:
  7 of 7 assertions passed

- Firestore rules structural delimiter validation
- New playoffWindowBanks rule path verified
- ZIP integrity validation
- Package file manifest verification

PRE-EXISTING WARNING
Angular reports an existing NG8113 warning for the unused RouterLink import in:
src/app/features/cycles/cycle-asset-detail/cycle-asset-detail.ts

That file is unchanged by this package and the warning does not block template
or TypeScript compilation.

FULL ANGULAR CLI BUILD
The packaging environment uses Node v22.16.0, below Angular 22's full CLI build
minimum. Therefore `ng build` was not claimed. TypeScript and Angular's template
compiler both passed. Run the normal local build using the project's known-good
Node v22.23.1 after installation.

FIRESTORE RULES
The rules received structural validation. Deploy the included rules to the live
project before testing bank documents.
