FANTASY HOCKEY — LIVE MODE + PROJECTION CALIBRATION V1

1. Stop the Angular development server.
2. Extract this package into the project root:
   /Users/StephenH/Documents/Programming/fantasy-hockey
3. Allow the five included source files to replace/add files at their exact paths.
4. Restart the app:
   npm start
5. Open Release Readiness or Projection Lab and use Regenerate Projection once.
   This publishes a Version 6 shared snapshot immediately. Otherwise the app
   will automatically generate Version 6 when the next roster-slot window opens.
6. Run:
   npm run build

No Firebase rules, indexes, dependencies, Cloud Functions, or migration are required.

EXPECTED RESULT
- The global historical-scoring warning banner is gone in local development.
- Local scoring now uses the live NHL date/season.
- Temporary deterministic developer tools remain available locally for now.
- New six-game windows use the less-conservative Version 6 calibration.
- Existing frozen windows do not change.
